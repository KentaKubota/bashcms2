#!/bin/bash -ex

source "$(dirname $0)/conf"

#dd:GitHUｂからくるデータを捨てる
[ -n "${CONTENT_LENGTH}" ] && dd bs=${CONTENT_LENGTH} > /dev/null

#HTTPヘッダの出力
echo -e 'Content-type: text/html\n\n'

#$contentsdir=/var/www/bashcms2_contentsに移動してgit pull
cd "$contentsdir"
git pull
